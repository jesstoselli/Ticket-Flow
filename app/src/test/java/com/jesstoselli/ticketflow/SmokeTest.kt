package com.jesstoselli.ticketflow

import org.junit.Assert.assertEquals
import org.junit.Test

class SmokeTest {
    @Test
    fun projectTestRuntimeIsConfigured() {
        assertEquals(4, 2 + 2)
    }
}
